# Oneinstack Panel 项目现状与上线开发路线图

> 审计日期：2026-07-27
> 后端仓库：`Oneinstack-Panel`，基线提交 `21775e1`
> 前端仓库：`Oneinstack-Panel-Web`，基线提交 `b871fe06`

## 1. 结论

当前项目已经具备管理面板的页面骨架、后端分层结构和一部分管理能力，但还不满足直接部署到公网生产服务器的条件。

两个仓库当前都可以完成基础编译：

- 后端 `go build ./...` 通过。
- 后端 `go test ./...`、软件任务/脚本/处理器等重点包 `go test -race`、`go vet ./...` 和生产二进制构建通过，当前已有 232 个测试函数。
- 前端 `vue-tsc --noEmit` 通过。
- 前端 Vitest 2 个单元用例通过。
- 前端 Vite 生产构建通过。

编译通过不能代表功能可用或安全。服务启动、健康探针、默认 IP/HTTP 与可选 HTTPS 面板访问、可信代理和原子配置、管理路由鉴权、用户敏感响应、文件根目录逃逸、容量保护、回收站、终端默认关闭、服务端会话吊销、首次登录强制改密、TOTP/恢复码、实例 JWT/凭据密钥、前端嵌入、安全安装器、软件持久化任务、四组件运行状态/服务控制/结构化配置、HTTP/HTTPS 网站配置事务、网站安全删除与整站备份、ACME 证书生命周期、MySQL 生命周期与备份恢复、计划任务安全模板与主动取消、防火墙补偿事务、签名更新/失败恢复、持久化防篡改审计、监控通知告警、实时运行日志和供应链发布制品已经完成第一轮整改；配置历史手动恢复、组件健康告警、访问配置自动重启/失败恢复、高级 Shell 低权限隔离、防火墙/更新实机矩阵、发布密钥轮换、真实公共 CA/Nginx/MySQL 和真实干净系统首次验收等问题仍然阻断上线。

综合现有代码，建议将当前状态定义为：

| 维度 | 估计状态 | 说明 |
| --- | --- | --- |
| 页面和交互模板 | 约 77% | 主要页面已经存在，面板访问、监控告警、运行日志、计划任务和整站备份已接入真实数据，部分页面仍是演示数据或静态表单 |
| API 和数据模型骨架 | 约 75% | 路由、模型、服务层、会话、审计、告警、运行日志、计划任务、网站任务与访问配置模型已形成，仍存在未完成或未连通逻辑 |
| 核心功能可靠性 | 约 60% | 核心建站、数据库、网站备份、防火墙、更新、审计、账号安全、面板访问、监控告警、运行日志和计划任务已有状态/补偿/持久化保护 |
| 自动化测试 | 约 79% | 已有 232 个 Go 测试函数、14 个 Bats 用例和 2 个前端单元用例；仍缺真实防火墙/MySQL/公共 ACME/systemd/断电恢复和浏览器 E2E |
| 安全与上线准备度 | 约 87% | 已关闭鉴权、文件、网站删除路径、会话吊销/首次登录/MFA、终端、数据库明文、面板代理边界/可选 TLS、SSL、防火墙事务、计划任务模板/取消、签名更新、审计、运行日志脱敏、Webhook SSRF 和安装器高风险默认值；高级 Shell 隔离、实机矩阵与密钥运维仍阻断 |
| 综合生产就绪度 | 约 77% | 核心建站与上线工程持续成形，仍只适合开发预览和隔离验收，不适合作为公网生产发布版 |

以上比例用于版本规划，不是精确工时统计。

## 2. 建议的 v1.0 产品范围

项目的长期目标已经明确为“宝塔级服务器管理能力 + OneinStack Shell 环境安装内核”。完整产品功能地图、Shell 任务架构和版本路线见 `PRODUCT_BLUEPRINT.md`。

为了尽快形成一个可维护、可升级、可直接部署的首个稳定版本，v1.0 先收敛为单机服务器管理面板：

- 首发系统：Ubuntu 22.04 LTS、Ubuntu 24.04 LTS。
- 首发架构：amd64；arm64 在首发稳定后纳入兼容矩阵。
- 使用场景：单台服务器、单管理员；默认通过服务器 IP 同源 HTTP 访问，可选配置独立 HTTPS。
- 首发组件：Nginx、MySQL 8.0、Redis 7、PHP 8.1/8.2/8.3。
- 首发能力：登录、安全设置、系统监控、网站、文件、数据库、防火墙、计划任务、软件安装、日志、备份、升级和回滚。
- Web 终端默认关闭；启用时需要二次认证、短期票据和完整审计。
- Docker、更多项目运行时和开放插件在核心建站闭环稳定后进入 0.5/1.x。
- Android/Capacitor 客户端、集群、多租户和复杂 RBAC 不纳入首个稳定版本。

“v1.0 收敛”不代表最终只做这些功能，而是先建立可上线的 Core 闭环，再沿产品蓝图扩展到 Docker、插件、多节点、安全防护和商业化能力。

## 3. 当前架构

### 3.1 后端

- Go、Gin、GORM、SQLite、Cobra、Viper。
- 主要目录：
  - `router/`：HTTP 路由、处理器、中间件、请求输入。
  - `internal/services/`：网站、数据库、软件、任务、日志、SSH 等服务。
  - `internal/models/`：数据库模型。
  - `app/`：配置、数据库和初始化。
  - `cmd/`：CLI 与服务入口。
- 当前进程同时承担：
  - Web API。
  - 静态页面。
  - root 级系统操作。
  - 定时任务。
  - 软件安装与卸载。
  - WebSocket 终端和 SSE 实时运行日志。

长期建议拆分为低权限 API 进程和受控的特权执行服务。特权服务只接受白名单化的结构化操作，不接受任意 shell 文本。

### 3.2 前端

- Vue 3、TypeScript、Vite 5、Element Plus。
- 页面覆盖监控、网站、文件、数据库、软件、防火墙、任务、终端、日志和系统设置。
- API、上传和 WebSocket 已改为同源运行时地址，不再依赖固定外网地址。
- 浏览器登录已使用 HttpOnly SameSite Cookie，前端不再持久化 JWT。
- `node_modules` 和 `dist` 被提交进仓库，导致仓库体积大、依赖来源和构建产物不可控。
- Android 目录属于 Capacitor 工程骨架，尚未达到发布和安全验收标准。

### 3.3 前后端交付

当前前端生产 ZIP 已通过 `go:embed` 嵌入后端，单个二进制可以同时提供 API 和 SPA。同步脚本会校验前端归档并原子替换嵌入产物；API 404、资源 404 和 SPA fallback 已有回归测试。发布包通过统一 Shell 安装器写入 systemd，初始化密码不进入命令参数或日志。

v1.0 继续保留两个仓库。当前已建立双仓库 CI、CodeQL、Dependabot、标签发布和 SHA-256 校验，后续继续完善：

1. 固定前端和后端版本标签。
2. 前端在 CI 中构建不可变的 `dist` 产物。
3. 后端通过现有 `go:embed` 流程嵌入经过校验的前端产物。
4. 已生成 SHA-256、签名更新清单、后端/前端 CycloneDX SBOM、依赖清单和许可证证据；下一步增加来源证明和密钥轮换/撤销演练。
5. 已配置 Ubuntu 22.04/24.04 安装、启动、健康和卸载矩阵；签名更新与自动回滚代码闭环已完成，真实升级故障矩阵待补充。

## 4. 模块开发进度

| 模块 | 当前状态 | 已有能力 | 主要缺口 |
| --- | --- | --- | --- |
| 登录与用户 | 核心安全闭环完成 | 登录、统一鉴权、HttpOnly Cookie、服务端会话吊销、改密全失效、首次登录强制改密、TOTP/恢复码、CSRF、实例密钥和设备管理 | 多管理员强制 MFA、Passkey/WebAuthn、凭据密钥轮换演练 |
| 系统监控 | 核心闭环完成 | CPU/内存/磁盘/负载/网络/I/O 分钟级历史、保留清理、阈值状态机、静默/恢复和趋势页 | Linux 长时间运行、服务/证书/网站可用性指标、降采样聚合 |
| 网站管理 | HTTP/HTTPS 与整站备份核心闭环完成 | 严格输入校验、原子发布、`nginx -t`、ACME HTTP-01、证书任务、自动续签、强制 HTTPS、HSTS、安全删除、删除前快照、文件/配置/可选 MySQL 整站备份恢复、取消/校验/保留和前端管理 | 缺少 DNS-01、访问策略、站点日志和真实公共 CA/Nginx/MySQL/断电集成矩阵 |
| 文件管理 | 安全与回收站闭环完成 | 授权根目录、容量配额、磁盘预留、回收站页面/调度、上传、下载、恢复、权限、读取、保存、受控 URL 下载和路由级审计 | 缺少分片上传、站点级配额和文件对象级差异审计 |
| 数据库管理 | MySQL 核心闭环完成 | 凭据加密/脱敏、连接测试、创建/用户授权/删除、持久化备份恢复任务、校验下载、恢复前安全备份、保留清理和前端进度 | 待真实 MySQL 8 矩阵、定时备份、S3、导入和凭据轮换；Redis 明确只提供逻辑库/键浏览 |
| 防火墙 | 代码闭环完成，待实机验收 | UFW/firewalld/iptables 统一状态与规则、输入白名单、持久化检查、补偿回滚、面板端口防自锁、端口变更预保护、ICMP 原子回滚和完整前端 | Ubuntu/Rocky 实机矩阵、IPv6/nftables、外部规则导入与对账 |
| 计划任务 | 安全执行核心闭环完成 | 正确启停、手动执行、Cron 校验、超时/取消进程组终止、同任务 forbid 互斥、最小环境、3 个无 Shell 白名单模板、结构化参数、高级 Shell 显式确认、重启恢复、日志保留/筛选/导出、失败通知和前端状态 | 高级 Shell 仍继承 Panel 权限；待更多模板、低权限执行器和真实 Linux 矩阵 |
| 软件管理 | 分发、任务、服务控制与配置发布闭环完成 | 独立 Center、不可变包、Ed25519、可信缓存/本地回退、四组件脚本、安装/升级/卸载/启停/重启/重载/配置持久化任务、状态/版本探测、差异预览、版本快照、原子发布、自动回滚、进度、取消、恢复、日志和统计 | 首发四组件尚未取得真实 systemd/配置 Linux 矩阵结果；历史手动恢复、健康告警和更多组件待补 |
| Web 终端 | 默认关闭并完成首轮加固 | 同源 WS、二次认证、一次性票据、Origin/输入/会话限制 | 启用后仍继承面板进程权限；缺少特权隔离、并发限额和命令级审计 |
| 审计日志 | 核心闭环完成 | HMAC 只追加链、链头截断检测、保留检查点、脱敏采集、管理员筛选/详情/导出、自动清理和前端页面 | 外部不可回滚锚点、对象级差异摘要和远端不可变归档 |
| 实时运行日志 | 核心闭环完成 | SQLite 保留、异步批量写入、凭据脱敏、稳定游标、SSE 续传、统计筛选和管理员前端控制台 | journald/Nginx/PHP/MySQL 适配、诊断包、追踪 ID 及 Linux 长连接/大流量矩阵 |
| 系统设置 | 访问配置首轮闭环完成 | 标题、用户名、改密/MFA/设备会话、默认 IP/HTTP、独立可选 HTTPS、证书校验、可信代理、候选端口、防火墙预保护、原子配置和前端页面 | systemd 自动重启/失败恢复、运行中无损切换和实机代理/IP SAN/IPv6 矩阵 |
| 通知与告警 | 核心闭环完成 | 告警事件、持续提醒、恢复通知、加密公网 HTTPS Webhook、HMAC 签名、测试发送、投递记录和前端管理 | 邮件/钉钉/飞书/企业微信原生适配、持久化重试/死信和外部端点故障矩阵 |
| 更新与回滚 | 代码闭环完成，待实机验收 | Ed25519 清单、制品校验、迁移预检、快照、双版本原子切换、健康确认、失败/中断恢复、CLI/API/前端进度 | Ubuntu 真实 systemd 升级/断电矩阵、发布密钥轮换/撤销演练和来源证明 |
| 安装部署 | 安全安装链路代码完成 | 前台服务、健康探针、主服务/独立更新 systemd 单元、双架构包、SHA-256、签名清单、SBOM、许可证证据、Bats 和 Ubuntu CI 矩阵 | 等待安装与升级矩阵首次远程结果；缺来源证明和发布密钥运维演练 |
| 国际化 | 部分完成 | 语言资源结构 | 大量页面仍硬编码中文，资源覆盖不完整 |
| 移动端 | 工程骨架 | Capacitor Android 目录 | 无签名、发布、权限、安全和功能验收流程 |

## 5. 上线阻断问题

### P0-01 高权限路由没有正确挂载鉴权

状态：已关闭（2026-07-24）。

`router/router.go` 在创建 `storageg`、`ftpg`、`softg`、`websiteg`、`safeg` 和 `crong` 后，错误地反复调用了 `sys.Use(...)`，而不是对对应路由组调用 `Use(...)`。

这会使数据库凭据、文件操作、软件安装、网站配置、防火墙和计划任务接口处于未认证状态。该问题必须作为首个修复项，并通过路由级自动化测试防止回归。

### P0-02 文件管理没有根目录边界

状态：已关闭（2026-07-24）。

`router/handler/ftp/ftp.go` 仅清理输入路径，没有把结果限制在允许的网站目录内。读取、上传、保存、递归删除、chmod、chown 和 URL 下载都可以作用于任意绝对路径。

整改要求：

- 使用服务端生成的站点根目录标识，不接受任意服务器绝对路径。
- 使用 `filepath.Rel`、真实路径解析和软链接检查确保目标位于授权根目录。
- 禁止访问系统目录和面板自身数据目录。
- URL 下载限制协议、目标地址、响应大小、超时和重定向，阻止内网 SSRF。
- 删除默认进入可恢复的回收站。
- 写入前执行容量配额和磁盘最低预留检查，并对并发写入进行容量预留。
- 回收站具有可配置保留期、后台清理调度和前端完整管理页面。

### P0-03 服务启动命令会立即退出

状态：已关闭（2026-07-24）。

`cmd/main.go` 的 `startServer()` 在 goroutine 中运行 Gin，随后函数和主进程直接返回。安装脚本和 systemd 都调用该命令，因此发布后服务无法稳定驻留。

同时存在以下关联问题：

- 监听端口固定为 `8089`，设置页面修改端口不会生效。
- `app.Viper()`/`server.Start()` 没有接入主入口，`app.ONE_CONFIG` 不能可靠加载。
- PID 文件记录的是即将退出的前台进程。

### P0-04 静态页面接入不完整

状态：已关闭（2026-07-24）。

旧 base64 生成器和未赋值函数指针已删除。前端生产 ZIP 现在通过 `go:embed` 进入单个 Go 二进制，并具有 SPA、静态资源和 API 404 边界测试。

### P0-05 Web 终端等价于远程 root shell

状态：公网直接暴露路径已关闭（2026-07-24）；特权进程隔离作为后续安全增强。

历史实现允许任意 Origin、将长期 JWT 放入查询参数并固定明文外网 WebSocket 地址。现已默认关闭终端；启用时要求密码二次认证和绑定用户/IP 的 30 秒一次性票据，并执行同源 Origin、输入大小、空闲时间和最大会话限制。

在完成权限隔离前，生产构建应默认禁用终端功能。后续启用至少需要：

- 同源 `wss://`。
- 一次性、短时效、单用途票据。
- 二次认证和明确的管理员授权。
- Origin 校验、空闲超时、并发限制。
- 会话开始/结束、来源 IP 和操作记录审计。

### P0-06 固定 JWT 密钥和敏感信息泄漏

状态：已关闭（2026-07-26）。固定 JWT、前端 Token、用户密码响应和数据库明文凭据均已整改。

- 首次启动会生成实例独立 JWT 密钥并以 `0600` 保存，已知旧示例密钥自动轮换。
- JWT 初始化不再写当前目录或打印生成的密钥。
- `models.User.Password`、`models.Storage.Password` 和 `models.Library.Password` 均禁止 JSON 序列化。
- 数据库实例与库用户密码使用实例级 AES-256-GCM 密钥加密，旧明文记录启动时事务迁移；API 只返回 `passwordConfigured`。

发布安装时必须生成每台机器唯一的密钥，敏感响应使用专用 DTO，并对需要保存的连接密码进行系统密钥加密。

### P0-07 软件安装功能没有可执行脚本

状态：分发和执行架构已关闭（2026-07-24）；生产组件包仍是上线阻断项。

安装器从相对路径 `scripts/install/<name>.sh` 读取安装脚本，但仓库中没有 `scripts/install` 目录。当前软件安装主流程会返回 “script not found”。

现已新建 `Oneinstack-Center` 并把安装主流程改为：Center 就绪检查、解析兼容包、验证预置 Ed25519 公钥/签名/SHA-256/大小/包内文件、安全缓存，然后执行预检查、安装、配置、验证和失败回滚；远程失败时使用发布包内置组件，旧内置脚本仅作为迁移期最后回退。因此原始 “script not found” 主路径已经消除，但旧脚本尚未全部重构成可验收的独立生产包。

软件脚本必须：

- 进入版本控制并按软件、版本和系统进行矩阵管理。
- 在执行前验证系统、磁盘、端口和已有服务。
- 支持幂等执行、失败回滚和中断恢复。
- 固定下载版本、镜像来源、SHA-256 或签名。
- 不从未签名的远程接口直接执行脚本。

### P0-08 网站配置可能造成配置损坏或误删目录

状态：HTTP 站点配置发布阻断已关闭（2026-07-24）；SSL/ACME 核心闭环已关闭（2026-07-26）；真实公共 CA/Nginx 集成矩阵待执行。

- 新增网站时以 `param.Name` 查询 `domain`，字段含义不一致。
- 数据库写入先于 Nginx 配置验证，失败后没有回滚。
- 删除网站时使用 `WebPath + w.Name`，而创建时使用的是 `WebPath + param.Dir`。
- Reload 前没有执行 `nginx -t`。
- 用户输入直接拼接到配置模板，缺少严格校验。

现已完成域名、路径和反向代理输入校验，配置采用同目录临时文件原子替换，并在 reload 前强制执行 `nginx -t`。新增、修改、删除与数据库更新进入补偿事务；任何配置测试、reload 或数据库提交失败都会恢复旧配置并重新加载。删除站点默认保留业务目录和文件。ACME HTTP-01、证书持久化任务、TLS 1.2/1.3、自动续签、强制 HTTPS/HSTS、过期状态和部署回滚已形成闭环。

### P0-09 构建和部署说明与仓库不一致

状态：构建说明、工作流、签名更新和代码级自动回滚已关闭；真实升级矩阵和来源证明待完成。

- 已重写 `BUILD.md`、Makefile、CI、CodeQL 和标签发布流程。
- 统一安装器不会替换软件源、全量升级系统、关闭防火墙或写入内核参数。
- 发布包附带 SHA-256；Release 生成 SBOM、模块清单、许可证证据和绑定制品摘要的 Ed25519 签名更新清单。
- 更新器完成数据库迁移预检、配置/数据库/脚本快照、双版本切换、健康确认和失败/中断恢复。
- 仍需加入来源证明、发布密钥轮换/撤销演练，并取得真实 systemd 升级矩阵结果。

### P0-10 操作审计只输出控制台

状态：核心阻断已关闭（2026-07-26）；外部不可回滚锚点作为后续增强。

- 新增只追加审计事件、签名保留检查点和签名链头，使用实例密钥派生的 HMAC-SHA-256 子密钥。
- 记录登录、未认证/权限/限流失败、所有变更操作与敏感读取；成功的普通读取不持久化。
- 请求正文和查询字符串永不进入审计库，字段长度、控制字符、来源代理和 CSV 公式注入均有服务端限制。
- 管理员可以分页、筛选、查看详情、导出 CSV 和手动校验完整性；普通用户不能访问审计 API。
- 默认保留 365 天并由 Cron 清理；清理前校验全链，清理后用签名检查点保持连续性。
- 内容篡改、链断裂、检查点篡改和尾部删除均有自动化测试；异常时拒绝追加和清理。
- 当前仍需把链头定期锚定到外部不可变存储，才能进一步识别攻击者连同整套数据库快照一起回滚的场景。

### P0-11 无状态会话无法吊销且首次登录不受限制

状态：核心阻断已关闭（2026-07-26）；凭据密钥轮换与多管理员策略作为后续增强。

- JWT 现在绑定数据库中的随机会话 ID 和用户安全版本；服务端不保存令牌本体。
- 鉴权会同时校验 JWT、用户安全版本、会话到期和吊销状态，旧无状态 JWT 被拒绝。
- 退出、人工吊销、密码修改和停用 TOTP 都能让对应会话立即失效，不再等待 24 小时 JWT 到期。
- 初始管理员及新建账号默认必须修改初始密码；完成前所有管理接口由中间件统一拒绝。
- 密码变更要求当前密码，事务内更新密码/安全版本并吊销全部会话。
- TOTP Secret 加密落库，恢复码只保存 keyed HMAC 摘要并单次消费；重复使用同一 TOTP 时间计数器会被拒绝。
- 前端已提供两步登录、强制改密、二维码启用、恢复码保存和有效设备管理。
- 会话/MFA 变更进入持久化审计；密码、动态口令、恢复码和 Secret 不进入审计正文。

生产运维仍需备份并演练轮换实例凭据密钥；v1.1 再增加组织级强制 MFA、Passkey/WebAuthn 和管理员重置 MFA。

## 6. 已确认的功能逻辑缺陷

这些问题不是代码风格问题，而是会直接影响功能结果：

1. 已关闭：统一使用 `userId`，用户名和密码修改已有集成测试。
2. 已关闭：计划任务启停会同步更新数据库与调度器，并有自动化测试。
3. 已关闭：新建 MySQL 数据库会真实创建数据库、专用用户和授权，失败执行补偿清理。
4. 已明确边界：Redis 不支持通过面板创建/删除逻辑库，仅提供连接、逻辑库统计和键浏览。
5. 已关闭：网站删除使用受管根路径与原子暂存，目录错误会阻止任务并回滚；删除前强制生成可校验整站快照。
6. 软件安装状态修改部分只更新内存对象，没有持久化。
7. 已关闭：删除旧 WebSocket 原型，运行日志订阅使用互斥保护、有界通道和慢客户端断开恢复。
8. 已关闭：安装进度由持久化软件任务的结构化事件负责；运行日志独立使用 SQLite 游标，不再从普通文本猜测进度。
9. 已关闭：前端各构建环境统一使用同源 `/v1`。
10. 已关闭：生产 API 不再指向演示域名。
11. 已关闭：终端使用同源 WS 和一次性票据，不再输出或在 URL 中携带长期令牌。
12. 已关闭：前端软件卸载已接入持久化任务、实时进度、取消和日志，失败时保留已安装状态。
13. 已关闭实时运行日志与通知页面缺口；面板默认 IP/HTTP、可选 TLS、可信代理与配置事务已完成，自动重启/失败恢复仍待完成。
14. 已关闭更新 CLI 占位：`check/apply/status/rollback` 已实现；端口变更 CLI 仍是 TODO。

## 7. v1.0 必须补充的产品需求

### 7.1 安全

- 所有 API 默认需要认证，仅登录、健康检查和必要的初始化接口进入白名单。
- 所有系统变更接口要求管理员权限、CSRF/Origin 防护和操作审计。
- 登录限流、密码强度、首次登录改密、会话失效、密钥轮换。
- 前后端保持同源；默认允许服务器 IP 上的 HTTP，HTTPS 为可选独立入口。配置安全响应头和可信代理列表，不使用会让 HTTP 失效的 HSTS。
- 所有外部下载使用固定允许列表、校验和、超时和大小限制。
- 敏感数据不进入日志、URL、API 响应或前端持久化明文。
- 建立安全扫描：Go 漏洞、npm 依赖、静态分析、镜像和 SBOM。

### 7.2 可靠性

- 系统操作采用“预检查 → 生成临时配置 → 校验 → 原子替换 → 重载 → 失败回滚”。
- 网站、数据库、证书、任务和面板配置支持自动备份与恢复。
- 安装、卸载、更新过程可重试、可恢复并提供结构化进度。
- 每次更新先进行数据库迁移检查和配置备份。
- 系统重启后任务、安装状态和日志状态可恢复。

### 7.3 可运维性

- `/health/live` 和 `/health/ready`。
- 结构化日志、日志轮转、错误追踪 ID。
- 操作审计日志已支持管理员查询/导出、HMAC 完整性校验和保留清理；下一步增加外部不可回滚链头锚点。
- 版本、构建提交、前后端版本和系统兼容信息可在面板中查看。
- 提供诊断包导出，但默认脱敏。

### 7.4 功能完整性

- 网站：域名、反向代理、PHP、SSL、Let's Encrypt、续签、重写、访问日志、备份恢复。
- 文件：限定根目录、编辑、上传、下载、回收站、压缩解压和权限管理。
- 数据库：创建、删除、用户授权、备份、恢复和连接测试。
- 防火墙：按支持系统使用唯一后端，变更前创建回滚点，防止关闭管理端口。
- 任务：脚本模板、启停、手动运行、超时、并发策略、日志和通知。
- 软件：版本矩阵、安装、卸载、升级、失败回滚和状态探测。
- 告警：CPU、内存和磁盘历史阈值及安全 Webhook 已完成；服务状态、证书过期和网站可用性继续接入同一规则引擎。

## 8. 分阶段开发计划

### 阶段 0：冻结基线与建立工程门禁

目标：后续每次提交都有可验证的质量基线。

- 清理前端仓库中的 `node_modules`、`dist` 和不应入库的产物，补齐 `.gitignore`。
- 明确并锁定 Node、npm 和 Go 版本。
- 建立后端单元测试、HTTP 路由测试、前端单元测试和最小 E2E 框架。
- 增加 CI：格式、lint、类型检查、构建、测试和安全扫描。
- 定义统一 API 响应、错误码和 OpenAPI 文档。
- 建立开发、测试和生产三套配置规则，禁止硬编码地址。

完成标准：两个仓库在全新环境中可以一条命令完成校验和构建。

### 阶段 1：清除 P0 安全和启动阻断项

目标：形成可安全启动的内部预览版。

- 重构程序入口、配置加载、信号处理和优雅关闭。
- 修复所有路由组鉴权，建立默认拒绝的权限策略。
- 修复用户上下文和敏感字段响应。
- 限制文件系统访问、容量并关闭不安全的 URL 下载。
- 生产环境默认禁用 Web 终端；启用路径使用二次认证和短期票据。
- 生成每台机器唯一的 JWT 密钥。
- 使用 `go:embed` 接入前端。
- 删除所有固定 API、WebSocket 和演示地址。

完成标准：未认证请求无法访问任何管理资源；服务可由 systemd 稳定启动、停止和重启；基础安全回归测试通过。

### 阶段 2：修复核心业务闭环

目标：所有首发模块都不是“只保存面板记录”，而是真正完成系统操作。

- 网站配置原子写入、校验、重载和回滚。
- 数据库创建、删除、授权、备份和恢复。
- 计划任务启停、并发策略、超时和执行日志。
- 防火墙系统适配、管理端口保护和回滚。
- 软件安装脚本矩阵、校验、进度、状态探测和失败恢复。
- 文件回收站、上传限额和软链接防护。
- 前端补齐真实日志、设置、通知和错误处理。

完成标准：每个模块都具有成功、失败、回滚和权限四类集成测试。

### 阶段 3：上线工程

目标：产出可重复安装、升级和回滚的发布包。

- 编写非破坏性安装器，禁止默认更换软件源、全量系统升级和关闭防火墙。
- 建立版本化配置和数据库迁移。
- 完成备份、恢复、更新、失败回滚和卸载。
- 发布包签名、SHA-256、SBOM 和变更日志。
- 修订 systemd 权限；逐步拆分 API 与特权执行服务。
- 建立前后端统一版本发布流水线。

完成标准：在干净系统中可重复执行“安装 → 配置 → 升级 → 回滚 → 卸载”，且不会破坏系统已有配置。

### 阶段 4：兼容性与发布验收

目标：形成 v1.0 稳定候选版本。

- Ubuntu 22.04/24.04 × amd64 完整矩阵测试。
- Nginx、MySQL 8、Redis 7 和 PHP 首发版本组合测试。
- 权限绕过、路径穿越、SSRF、WebSocket、命令注入和密钥泄漏专项测试。
- 故障注入：磁盘满、下载中断、配置校验失败、服务重启和数据库损坏。
- 性能测试：页面加载、日志流、文件上传、指标刷新和任务并发。
- 小范围真实服务器灰度，收集安装与升级结果。

完成标准：所有 P0/P1 缺陷关闭，发布检查表通过，有可验证的备份和回滚方案。

### 阶段 5：v1.1 及以后

- arm64、Debian 和 Rocky Linux 支持。
- 多管理员和正式 RBAC。
- 多主机管理。
- Android 客户端。
- 插件市场和第三方扩展。

这些项目不应阻塞首个稳定单机版本。

## 9. 第一批开发任务

建议按以下顺序直接开始：

| 顺序 | 任务 ID | 状态 | 任务 | 交付结果 |
| --- | --- | --- | --- | --- |
| 1 | DEV-P0-01 | 已完成 | 建立路由安全测试并修复路由组鉴权 | 未登录访问所有管理接口均返回 401 |
| 2 | DEV-P0-02 | 已完成 | 重构服务入口和配置加载 | `one server start` 前台稳定驻留，支持信号退出和配置端口 |
| 3 | DEV-P0-03 | 已完成 | 修复用户上下文和响应 DTO | 用户名/密码修改可用，所有响应不含密码哈希 |
| 4 | DEV-P0-04 | 已完成 | 文件服务、容量和回收站闭环 | 路径、软链接、SSRF、配额、磁盘预留、清理调度和前端回收站已完成 |
| 5 | DEV-P0-05 | 已完成（Root 模式） | Web 终端鉴权、审计与 Root 运行模式 | 无固定地址/URL JWT，使用二次认证票据和 HttpOnly 会话；终端进程直接以 root 启动并保留并发、空闲和时长限制 |
| 6 | DEV-P0-06 | 已完成 | 重建前端嵌入和发布构建 | 单个后端发布包稳定提供 API 和 SPA，并区分 API/资源/页面 404 |
| 7 | DEV-P0-07 | 待远程验收 | 建立 CI、健康检查和发布门禁 | 第二批代码完成；等待 Ubuntu 22.04/24.04 矩阵首次结果 |
| 8 | DEV-P1-01 | 已完成 HTTP/SSL 闭环 | 网站配置事务、ACME 与回滚 | 错误配置或证书不会写入或造成 Nginx 中断；真实公共 CA 矩阵待验收 |
| 9 | DEV-P1-10 | 已完成 | 服务端会话、首次登录保护与 TOTP | 会话可即时吊销，改密全失效，TOTP/恢复码和前端设备管理闭环 |
| 10 | DEV-P1-11 | 已完成首轮闭环 | 历史监控与通知告警 | 分钟级指标、阈值状态机、静默/恢复、加密 Webhook、投递记录和前端告警中心已完成 |
| 11 | DEV-P1-12 | 已完成首轮闭环 | 实时日志中心可靠性 | SQLite 持久化、脱敏、稳定游标、SSE 续传、保留清理和前端控制台已完成 |
| 12 | DEV-P1-13 | 已完成首轮闭环 | 计划任务安全模板与主动取消 | 三种无 Shell 白名单模板、结构化参数、高级 Shell 显式确认、精确取消、重启恢复、日志保留/导出和失败通知已完成 |
| 13 | DEV-P1-14 | 已完成首轮闭环 | 网站删除安全与整站备份 | 删除前强制快照、受管目录原子暂存、文件/Nginx 配置/可选 MySQL 备份恢复、取消/校验/保留和前端管理已完成 |
| 14 | DEV-P1-15 | 已完成首轮闭环 | 默认 IP/HTTP 与可选 HTTPS 面板访问 | HTTP 永久可用、HTTPS 独立可选、证书/端口/代理校验、原子配置、防火墙预保护、安全头和前端设置已完成 |
| 15 | DEV-P1-16 | 已完成首轮闭环 | 四组件服务控制与运行状态 | 严格状态/版本探测、启停/重启任务、Nginx/PHP-FPM 安全重载、权限审计和前端控制已完成 |
| 16 | DEV-P1-17 | 已完成首轮闭环 | 四组件结构化配置与安全发布 | 白名单字段、指纹/差异、版本快照、原生语法检查、原子发布、按能力生效、失败回滚和前端配置页已完成 |

第一批任务完成后再开始软件安装、数据库和防火墙等高权限功能，避免在不安全的基础上继续扩大功能面。

### 2026-07-24 开发记录

- 完成 `DEV-P0-01`：路由改为公开白名单和统一受保护组，所有管理 API 默认经过限流、JWT 鉴权和审计中间件。
- 新增路由回归测试，自动遍历所有 `/v1` 管理路由，验证无 Token 时返回 `401/MISSING_TOKEN`。
- 增加合法 Bearer Token 正常访问测试。
- SSH 路由不再接受查询参数中的旧 Token 绕过统一鉴权。
- 开始 `DEV-P0-02`：移除 `app` 包导入时写入 `/usr/local/one` 和初始化数据库的副作用，改为显式 `app.Initialize()`。
- Cron 调度器改为数据库初始化后显式/延迟启动，避免测试导入包时启动后台服务。
- 完成 `DEV-P0-02`：服务改为前台阻塞运行，读取配置端口，支持 SIGINT/SIGTERM 优雅关闭，并将 PID 文件放入面板数据目录。
- systemd 继续以 `Type=simple` 管理主进程，不再通过 `ExecStop` 启动第二个面板进程。
- 增加 HTTP 生命周期、端口校验、配置加载、环境变量覆盖和显式初始化测试。
- 完成 `DEV-P0-03`：统一用户上下文键，修复用户名和密码修改读取错误键的问题。
- 用户设置改用专用请求和响应 DTO，`models.User.Password` 禁止 JSON 序列化。
- 增加用户名修改、密码重置和系统信息不泄漏密码的集成测试。
- 开始 `DEV-P0-04`：建立基于 `os.Root` 的虚拟路径和授权根目录文件服务，迁移全部文件接口。
- 文件服务拒绝父目录穿越、物理绝对路径逃逸和越界符号链接，禁止删除或修改授权根目录。
- 上传限制 `100 MiB`，在线编辑限制 `10 MiB`，创建和下载失败不会覆盖或遗留部分文件。
- URL 下载仅允许 HTTP/HTTPS，并限制 DNS/目标地址、重定向、响应大小和超时，阻止本机、私网和云元数据 SSRF。
- 增加文件服务单元和接口测试，覆盖虚拟路径、软链接、根目录保护、SSRF、超大响应、异常状态和超时。
- 文件删除改为原子移动到内部回收站，新增列表、恢复、彻底删除、确认清空和按时间清理服务。
- 回收站恢复使用原子不覆盖语义；原位置存在新文件时拒绝恢复，目录和符号链接不会被跟随到授权根目录之外。
- 新增回收站服务和 API 测试，覆盖生命周期、恢复冲突、永久删除、清空、过期清理、内部目录隐藏和恶意内部软链接。
- 完成文件容量配额和磁盘最低预留；并发写入先预留容量，配额或磁盘不足返回 `507` 且不修改原文件。
- 回收站保留期和 Cron 清理计划进入配置，后台清理器随服务启动和停止。
- 前端新增回收站页面、容量信息、恢复、彻底删除和确认清空交互。
- 完成 `DEV-P0-05`：终端默认关闭，启用时要求密码二次认证、30 秒一次性票据、严格同源和会话限制。
- 前端 API/上传/WS 改为同源，移除固定演示地址、URL JWT 和浏览器可读 Token。
- 登录改用 HttpOnly SameSite Cookie；Cookie 写请求增加 Origin CSRF 校验，退出清理会话。
- 移除仓库固定 JWT，首次启动生成实例密钥并自动轮换旧示例密钥。
- 完成 `DEV-P0-06`：删除旧 base64 UI 和空函数指针，通过 `go:embed` 提供单二进制 SPA。
- 增加前端构建产物校验/同步脚本，修复 API 404、静态资源 404 和 SPA fallback 边界。
- 完成 `DEV-P0-07` 第一批：后端 CI/CodeQL/Dependabot、前端 CI/CodeQL/Dependabot、标签发布工作流和健康/版本接口。
- 完成 `DEV-P0-07` 第二批代码：统一非破坏性安装器、安全管理员初始化、7 个 Bats 用例、Ubuntu 22.04/24.04 生命周期矩阵、前后端 Secret Scan、SBOM 和许可证证据制品。
- 截至 2026-07-24 批次，自动化测试增加至 165 个 Go 测试函数、14 个 Bats 用例和 2 个前端单元用例；当前数量见 2026-07-26 记录。
- `go vet ./...`、`go test ./...`、`go test -race ./...` 和安装脚本 `bash -n` 检查通过。
- 前端 typecheck、Vitest 和生产构建通过；Linux amd64/arm64 静态构建及发布包 SHA-256/内容校验通过。
- 升级 `gopsutil v4.26.6` 和 `purego v0.10.2`，关闭 macOS ARM64 竞态测试运行时崩溃。

### 2026-07-26 会话安全开发记录

- 新增服务端会话登记表，JWT 绑定随机会话 ID 和用户安全版本；旧无状态 JWT 被拒绝。
- 退出、指定设备吊销、退出其他设备、密码变更和停用 TOTP 均可立即让对应会话失效。
- 初始管理员与新建账号默认强制改密，完成前由中间件阻止进入其他管理接口。
- 密码变更增加当前密码确认，并在事务内完成哈希更新、安全版本递增、首次登录标记清除和全会话吊销。
- 完成 RFC 6238 TOTP、加密 Secret、二维码注册、登录两步验证、计数器重放拒绝和单次恢复码。
- 前端新增首次登录改密页、两步登录以及动态口令/恢复码/有效设备安全管理页面。
- 该批次自动化测试增加至 169 个 Go 测试函数；监控告警批次完成后当前累计 177 个，全量 Go 测试、重点包 race、vet、前端类型检查、Vitest 和生产构建通过。
- 前端生产 ZIP 已同步到后端嵌入包，SHA-256 一致。

### 2026-07-26 实时日志开发记录

- 删除固定读取安装目录、无真实调用方且存在共享状态竞态的旧 WebSocket 日志原型。
- 新增 SQLite 运行日志、有界异步批量写入、优雅停止刷新、丢弃计数、凭据脱敏和可配置保留清理。
- 面板标准日志和不含查询字符串的 HTTP 访问摘要接入真实日志源。
- 新增管理员游标查询、统计和 SSE 接口，支持 `Last-Event-ID` 续传、慢客户端断开后回填及组合筛选。
- 前端新增运行日志控制台，支持实时状态、历史范围、向前翻页、暂停/恢复、自动滚动、复制和导出当前视图。
- 当前累计 206 个 Go 测试函数；全量测试、重点包 race、vet、前端类型检查、2 个 Vitest 用例和生产构建通过。
- 前端嵌入 ZIP SHA-256 更新为 `b5a6d8fd5eed96c02a52c9728101efee093be3f47a6563f53c0292e6fe96d0ec`。

### 2026-07-26 计划任务安全开发记录

- 新增安全模板任务类型和元数据接口，首批磁盘使用、服务状态、网站目录容量模板使用固定可执行文件及参数白名单，不经过 Shell。
- 高级 Shell 继续兼容，但创建/修改必须显式确认风险；执行环境保持最小化，输出新增凭据和控制字符脱敏。
- 每次运行在进程启动前登记执行 ID，前端可精确主动取消；超时、取消和停服均终止进程组，重启恢复遗留运行记录。
- 日志新增状态/时间筛选、CSV 防公式注入导出、默认 30 天自动清理和管理员手动清理，运行中记录不会被删除。
- 失败和超时可选复用监控中心的加密通知通道、事件与投递记录，不向外发送执行输出。
- 前端完成模板驱动表单、结构化参数、高级 Shell 风险确认、运行状态、主动取消、失败通知和日志管理。
- 当前累计 206 个 Go 测试函数；全量测试、重点包 race、vet、生产二进制构建、前端类型检查、2 个 Vitest 用例和生产构建通过。
- 前端嵌入 ZIP SHA-256 更新为 `93e395a19471dd3a18d4dc6fa180861d172b8efd43e496abd8bd33587007cad3`。

### 2026-07-26 网站删除与整站备份开发记录

- 网站删除改为必须精确确认的持久化任务，只有删除前整站快照创建并通过 SHA-256 校验后才执行配置与可选文件删除。
- 网站和证书目录使用受管根路径校验及私有暂存目录；Nginx/数据库失败会回移目录，目录删除错误不再被忽略。
- 新增网站备份/恢复/删除任务、资源锁、日志、进度、取消、重启中断、默认 30 天保留和活动恢复保护。
- 整站包包含网站元数据、Nginx 配置快照、网站文件逐项摘要和一个可选 MySQL 转储；证书私钥不进入未加密归档。
- Go 原生归档器拒绝目录穿越、绝对路径、重复/未知条目、危险符号链接、特殊文件和容量/条目上限超限。
- 恢复前强制生成安全快照，文件同文件系统原子切换，Nginx 配置通过当前安全模板重新生成，数据库凭据不进入归档或日志。
- 前端新增站点级和全局整站备份管理，支持数据库选择、进度/取消/日志、下载、恢复、删除，以及已删除网站恢复。
- 当前累计 206 个 Go 测试函数；全量测试、重点包 race、vet、生产二进制构建、前端类型检查、2 个 Vitest 用例和生产构建通过。
- 前端嵌入 ZIP SHA-256 更新为 `81f9731019c730a6e10c4c993c8f7fe9cb6f0757998772593ddd501e4c2b2e8e`。

## 10. Definition of Done

一个功能只有同时满足以下条件才算完成：

- 产品流程在前端可完整操作，不依赖手工修改数据库或配置文件。
- 服务端验证所有输入，不信任前端校验。
- 权限、成功、失败、超时、重试和回滚路径均有定义。
- 关键逻辑有单元测试，接口有集成测试，核心用户路径有 E2E 测试。
- 日志不包含密码、令牌、私钥或数据库密钥。
- 系统配置变更在应用前经过语法/可用性验证。
- 错误对用户可理解，对运维可诊断。
- 文档、配置示例、OpenAPI 和变更日志同步更新。
- 在支持系统的干净镜像中通过安装和升级测试。
- Code review 和 CI 全部通过。

## 11. 发布检查表

正式上线前至少确认：

- [ ] 所有管理 API 都有认证与权限测试。
- [ ] 不存在固定 JWT 密钥、固定域名、固定 IP 或测试账号。
- [x] 页面与 API 使用同源地址；默认 IP/HTTP 可用，HTTPS 可独立配置，生产代码不依赖固定外网地址。
- [x] 文件管理无法逃逸授权目录。
- [x] Web 终端默认禁用；启用入口已完成首轮独立安全验收。
- [ ] 软件包和远程脚本有固定版本及完整性校验。
- [x] Nginx、防火墙、数据库和任务变更支持代码级补偿回滚（真实环境矩阵仍待完成）。
- [x] 面板更新前可备份并恢复数据库、WAL/SHM、系统配置和内置脚本（真实故障矩阵待完成）。
- [x] 更新失败或中断可在代码级自动/手动回到上一版本（真实 systemd/断电矩阵待完成）。
- [x] 高权限操作、登录与失败请求已持久化审计，可由管理员查询/导出并校验 HMAC 链完整性。
- [x] 登录会话可服务端即时吊销，首次登录必须改密，TOTP/恢复码和改密全会话失效已完成。
- [ ] systemd 健康检查、日志轮转和资源限制有效。
- [x] 安装脚本不会关闭防火墙、替换系统软件源或执行无提示的全量升级。
- [ ] 支持系统矩阵和浏览器矩阵通过验收。
- [ ] 发布包已附带校验和、签名更新清单、SBOM 和许可证证据；来源证明、密钥轮换演练和变更日志自动校验仍待完成。

## 12. 开发前需要确认的产品决策

文档默认采用以下建议值，未提出不同要求时可据此开始开发：

| 决策 | 建议默认值 | 影响 |
| --- | --- | --- |
| 首发系统 | Ubuntu 22.04/24.04 amd64 | 决定安装、服务、防火墙和测试矩阵 |
| 用户模式 | 单管理员 | 最快形成稳定版；多管理员延后到 v1.1 |
| 暴露方式 | 默认服务器 IP + HTTP；可选独立 HTTPS，同源部署 | HTTP 不依赖域名/证书；公网环境仍必须按高风险管理面设计 |
| Web 终端 | v1.0 默认关闭 | 降低 root 远程执行风险 |
| 软件版本 | Nginx + MySQL 8 + Redis 7 + PHP 8.1/8.2/8.3 | 控制安装脚本和兼容性范围 |
| 移动端 | 不进入 v1.0 | 优先保证 Web 和服务端稳定 |
| 仓库策略 | 保留前后端双仓库，统一发布流水线 | 避免当前阶段进行高成本合仓 |

## 13. 本次开发与审计边界

本次已完成 Nginx、MySQL 8、PHP-FPM、Redis 的严格状态/版本探测、服务控制和结构化配置安全发布；配置变更提供白名单字段、指纹并发控制、差异预览、版本快照、原生语法检查、原子替换、按能力重载/重启和失败自动回滚，并接入持久化任务、权限、审计、实时进度、取消、日志和前端配置页。全量 Go 测试、前端类型检查/测试/生产构建、Center/内置包校验和 Shell 语法检查通过。当前开发机没有 systemd 和四组件运行环境，本轮没有实际变更宿主机服务配置。

整站归档当前为权限 `0600` 的本地文件但未做应用层加密，因此证书私钥不进入备份；恢复后需要重新签发证书。一个备份最多关联一个 MySQL 数据库，数据库恢复失败时保留恢复前安全快照供管理员回退，不宣称支持多数据库依赖图或数据库级自动回滚。

生产上线前仍需在 Ubuntu 22.04/24.04 + Nginx + MySQL 8 验证大文件、磁盘写满、长任务取消、符号链接、反向代理、Panel 强制终止与断电故障矩阵。文件切换极短窗口内的强制断电仍依赖已登记安全快照进行恢复演练。

下一代码开发项目为配置历史恢复与组件健康告警闭环：提供版本快照列表、字段差异和受审计的手动恢复任务，并把四组件停止、失败和恢复状态接入监控与通知中心。

当前审计保证面向“数据库内容被修改、删除或常规截断”的可检测性，不宣称能够抵抗同时取得实例密钥的攻击者，也不能单独识别整套数据库和配置被一起回滚。生产强化需要把周期性链头签名发送到独立不可变存储或安全中心。

TOTP Secret 依赖实例凭据密钥解密；生产交付必须把该密钥纳入受控备份、轮换和灾难恢复演练，不能只备份 SQLite 数据库。

依赖漏洞库扫描需要联网查询外部漏洞数据；当前仅确认锁文件和项目构建状态，不能据此认定依赖不存在已知漏洞。该扫描应进入 CI 和正式发布门禁。
