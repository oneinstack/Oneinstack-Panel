# OneinStack 项目开发进度表

> 更新时间：2026-07-27
> 维护规则：每完成一个独立开发项目，同步更新本表的状态、完成内容、未完成内容和验证结果。

## 当前进度

| 开发项目 | 状态 | 已完成 | 未完成 / 下一步 |
| --- | --- | --- | --- |
| 路由鉴权与会话安全 | 已完成 | 管理 API 统一鉴权、服务端会话登记/吊销、密码变更全会话失效、首次登录强制改密、TOTP、一次性恢复码、安全设置页面 | 密钥轮换运维、多管理员设备策略、Passkey/WebAuthn |
| 服务启动与健康检查 | 已完成首轮闭环 | 正确启动、配置加载、HTTP/可选 HTTPS 双监听、优雅关闭、live/ready 探针 | systemd 探针联动、配置自动重启与失败恢复 |
| 面板访问与可选 HTTPS | 已完成首轮闭环 | 默认 `0.0.0.0:8089` HTTP/IP 访问、独立可选 HTTPS、证书/私钥/有效期校验、候选端口预检、原子配置、可信代理、安全响应头、前端设置页 | systemd 自动重启协调、运行中无损换端口、真实 IP SAN/反向代理矩阵 |
| 文件、容量与回收站 | 已完成 | 文件根边界、配额、磁盘预留、并发容量预留、回收站页面、自动清理、路由级审计 | 分片上传、站点级配额、文件对象级审计 |
| Center 脚本与面板版本服务中心 | 核心控制面完成，待生产化 | 不可变组件包、Panel 双架构版本、发布/撤销、兼容解析、Ed25519、可信缓存、本地回退、管理控制台、viewer/publisher/admin Token、签名审计、百分比/实例灰度；Ubuntu 24.04 原生 Center 已运行 | HTTPS 反向代理、OIDC/审批、在线密钥轮换、PostgreSQL/S3、监控与备份恢复演练 |
| Nginx/MySQL/PHP/Redis 独立脚本 | Nginx/MySQL 已通过 Ubuntu 24.04 实机安装 | 安装、配置、验证、升级、回滚、卸载、状态、服务控制、结构化配置、版本快照和自动回滚；Nginx 1.28.2 与 MySQL 8.0.45 已从 Center 安装并通过 systemd/运行态验证 | Ubuntu 22.04 和重复安装/升级/故障矩阵；PHP/Redis 原生验收；Debian 与 ARM64 扩展 |
| 软件任务与组件服务控制 | 已完成首轮闭环 | 安装/升级/卸载/启停/重启/重载/配置发布持久化任务、状态与版本探测、事件、日志、SSE、取消、重启恢复；顶栏运行任务计数、任务列表、完成通知和页面自动刷新 | 真实 systemd 矩阵、更多组件、网站/数据库/计划任务统一汇入全局任务中心 |
| 组件配置管理 | 已完成首轮闭环 | 四组件白名单字段、指纹并发控制、差异预览、版本快照、原子发布、原生语法检查、按能力重载/重启、失败自动回滚和前端页面 | 历史快照列表/差异/手动恢复、服务健康告警、真实组件矩阵 |
| 网站、SSL 与整站备份 | 代码完成，待实机验收 | Nginx 原子配置、失败回滚、HTTP-01、证书任务、自动续签、HTTPS/HSTS、受管路径删除、删除前强制快照、文件/配置/可选 MySQL 整站备份恢复、取消/校验/保留和前端管理 | 公共 CA/Nginx/MySQL 实机矩阵、DNS-01、通配符证书、站点访问策略与日志 |
| MySQL 生命周期与备份恢复 | 核心生命周期实机通过 | 零参数安装、root 随机密码和加密本机连接；同名库账户、随机密码、二次认证查看/轮换、实连校验、删除；备份恢复任务、摘要和恢复前安全备份 | Ubuntu 22.04、备份恢复/故障注入矩阵、定时备份、S3 |
| 计划任务安全执行 | 已完成首轮闭环 | Cron 校验、超时、互斥、进程组终止、最小环境、3 个白名单模板、结构化参数、高级 Shell 显式确认、主动取消、重启恢复、日志脱敏/保留/筛选/导出、失败通知和完整前端 | 更多运维模板、低权限执行器/进程隔离、Linux systemctl 与进程终止实机矩阵 |
| 防火墙安全闭环 | Docker 安装/配置修复完成，待实机启用验收 | UFW/firewalld/iptables 统一模型、输入校验、持久化检查、补偿回滚、面板端口防自锁；默认 firewalld 安装；补齐协议数据库并执行离线配置校验；无 systemd 环境禁用启停并给出明确提示；Debian 12 ARM64 Docker 安装验证通过 | Ubuntu/Debian/Rocky/AlmaLinux 真实 systemd 启用矩阵、外部规则导入/对账、IPv6/nftables |
| Web 终端 | 已完成首轮加固 | 默认关闭、二次认证、一次性票据、同源 WebSocket、Origin 校验 | 低权限隔离、并发限额、命令级审计 |
| CI 与发布质量门禁 | 代码完成，待远程验收 | Go/前端测试、race、vet、CodeQL、SBOM、SHA-256、签名更新清单、双系统组件矩阵 | 首次 GitHub Runner 结果、签名密钥运维、来源证明 |
| 更新与自动回滚 | Center 管控链路完成，待实机升级验收 | Center 版本决策、持久实例 ID、渠道/最低版本/时间窗/百分比/名单灰度、撤回、Ed25519 签名、双架构制品校验、迁移预检、原子切换、健康确认、失败/中断恢复和前端进度 | Ubuntu 22.04/24.04 真实升级/断电故障矩阵、在线密钥轮换、自动健康门禁和来源证明 |
| 持久化审计日志 | 已完成 | HMAC 哈希链、链头/保留检查点、脱敏采集、登录与失败请求、管理员查询/详情/筛选、CSV 导出、自动清理、前端页面 | 外部不可回滚锚点、对象级差异摘要、归档到远端不可变存储 |
| 实时日志中心 | 已完成首轮闭环 | SQLite 持久化、异步批量写入、脱敏、游标分页、SSE 断线续传、保留清理、统计筛选、管理员前端控制台 | journald/Nginx/PHP/MySQL 日志适配、诊断包、浏览器长连接与大流量矩阵 |
| 监控与通知告警 | 已完成首轮闭环 | 分钟级历史指标、独立保留策略、阈值与恢复滞回、连续采样、提醒冷却、静默、触发/恢复事件、加密 Webhook、投递记录、管理员告警中心 | Linux 长时间运行验收、邮件/钉钉/飞书原生适配、服务状态与证书到期规则 |
| Docker 与插件系统 | 未开始 | 产品范围已进入蓝图 | 容器生命周期、Compose、插件 Manifest/权限/签名 |

## 最近完成项目

### Center 统一控制 Panel 版本（2026-07-27）

完成：

- Center 新增独立的 Panel 发布仓库，保存不可变 Linux amd64/arm64 制品和 Ed25519 签名原子索引，索引被修改后拒绝启动；支持草稿、发布、永久撤回、最低直升版本和发行说明。
- Center 按渠道、平台、当前版本、时间窗、稳定实例 ID、显式包含/排除和确定性百分比决定是否给 Panel 返回新版本；未命中时返回 `204`。
- Center 对每次解析出的 Panel 更新清单使用现有 Ed25519 密钥签名；Panel 使用固定公钥再次验证签名、目标平台、制品大小和 SHA-256。
- Center 的组件包索引也升级为 Ed25519 签名格式，旧索引启动时经过清单、签名和制品摘要验证后迁移；每次下载前重新核对文件类型、容量与 SHA-256。
- Panel 自动生成并私密保存稳定实例 ID，默认请求 `centerUrl`；未单独配置更新公钥时复用脚本 Center 信任公钥，静态 `manifestUrl` 继续作为兼容模式。
- Center 内置 `/admin/` 控制台，完成组件包、Panel 版本、双架构制品、发布、灰度、撤回、活动公钥和审计记录管理。
- Center 新增 hash-only scoped Token，角色分为 viewer、publisher、admin；管理变更写入 Ed25519 签名哈希链审计日志，启动时校验完整性。
- Panel Release 工作流上传双架构制品到 Center，初始灰度默认 `0%`；前端设置页显示 Center 来源和实例标识。
- 修复前端 Linux CI 中 `vite-var` 包文件名大小写不一致问题，改为仓库内受控构建插件。
- 最新内嵌前端 SHA-256：`1bc2ee4b5e4d9bde4bc48e9acf1afab93521e5171bb3a25c49101dddde30714f`。

验证：

- Center `go test -count=1 ./...`、核心包 race、`go vet ./...`、Linux amd64/arm64 静态编译和 `git diff --check` 通过，覆盖发布生命周期、签名、灰度、撤回、持久化、RBAC 和审计防篡改。
- Panel 全量 `go test -count=1 ./...`、更新模块 race、`go vet ./...`、发布脚本语法和差异检查通过；当前共有 246 个 Go 测试函数。
- 前端 `npm run typecheck`、Vitest 和生产构建通过，构建包已同步到 Panel `webui/app.zip`。
- 本地真实 HTTP 烟雾测试完成双架构上传、发布、签名解析、制品下载摘要校验，并验证同一版本/相同制品的 CI 重跑可安全幂等结束。

尚未完成：

- Center 目前仍是单节点文件存储；生产 HA 需要 PostgreSQL、S3 兼容对象存储、备份恢复演练、监控和限流。
- OIDC、多方审批、离线根密钥/在线轮换、签名撤销列表、SBOM/来源证明策略和自动健康回滚仍待开发。
- 需要在 Ubuntu 22.04/24.04 真实 systemd 主机完成 Center 灰度升级、失败回滚、服务中断和断电恢复矩阵。

### 软件商城状态一致性与 MySQL 零配置闭环（2026-07-27）

完成：

- 软件目录聚合改为明确选择已安装版本，Nginx 顶部状态、底部文案和按钮保持一致。
- MySQL 安装固定端口 `3306`、管理员 `root`，服务端生成 24 位随机密码并自动登记加密本机连接。
- 新建数据库时用户名等于数据库名、密码随机；列表不返回密码，查看和修改密码均要求当前面板密码。
- MySQL 账户改为 `数据库名@%`，创建与轮换后执行真实登录验证；失败时清理新资源或恢复旧密码。
- Center MySQL `1.0.6` 完成 Ubuntu 24.04 `libaio1t64`、下载校验缓存、socket 就绪等待和 `root@127.0.0.1` 管理连接修复。

验证：

- Ubuntu 24.04 amd64 上 MySQL 8.0.45 为 active/enabled，监听 `127.0.0.1:3306`。
- Panel 软件目录返回 MySQL `installed=true/status=2/install_version=8.0/params=null`，本机连接仅返回凭据已配置状态。
- 实机创建、查看、轮换、CLI 登录和删除验收通过；测试库和账户已删除。
- Panel `test-20260727.4` 已部署，Center 解析版本为 MySQL 包 `1.0.6`。

### Center 联调、Nginx 修复、firewalld 配置修复与弹窗统一（2026-07-27）

完成：

- firewalld Debian 安装强制补齐 `netbase`，RPM/zypper 安装对应协议数据库；安装、修复和验证阶段同时检查 `getent protocols esp` 与 `firewall-offline-cmd --check-config`。
- firewalld 状态新增 `canToggle/repairRequired`；配置损坏时提供修复任务，无 systemd 的 Docker 环境禁止启停并显示明确说明。
- Nginx 正式包新增 Debian 12、amd64/arm64 支持；APT 使用 HTTPS 临时源、重试和 `--fix-missing`，避免旧脚本依赖下载中断。
- Nginx 在真实服务器继续使用 systemd，在无 systemd 容器中使用受控直接进程模式；状态与维护动作共用统一运行时判断。
- Center Compose 增加最小权限数据卷初始化；服务以非 root 运行，已发布 Nginx `1.0.1` 及原四组件稳定包。
- Panel 本地 Docker 配置启用显式开发 HTTP、固定 Ed25519 公钥和 Center 地址；默认及生产配置仍强制 HTTPS。
- 全局对话框统一水平、垂直居中；安装任务抽屉统一主界面的颜色、圆角、卡片、步骤、日志和操作区。
- 最新内嵌前端 SHA-256：`746f6c699779023fd0c45a45eec1f645296e4d04e100f6c885190550ad2f722e`。

验证：

- Panel `app`、`safe`、`scriptregistry`、`software` 重点 Go 测试通过；firewalld/Nginx 脚本通过 `bash -n` 和 `files.sha256` 校验。
- 前端类型检查和生产构建通过，Docker Panel 健康检查通过。
- Panel 容器内成功解析并缓存 Center Nginx `1.0.1`；浏览器实装 Nginx 1.28.2 成功，用时约 19 秒。
- firewalld 在 Debian 12 ARM64 Docker 安装成功；`esp 50 IPSEC-ESP` 可解析，离线配置检查通过。

尚未完成：

- Docker 不运行 systemd，也不代表宿主机防火墙命名空间；firewalld 启用、端口保护和重启持久化仍需真实 Linux 主机验收。
- Center 当前为单节点 HTTP 本地联调；生产部署必须配置 HTTPS、备份签名私钥并完成密钥轮换/撤销演练。
- 软件商城展示目录仍来自 Panel 本地数据库；Center 当前负责安装脚本包解析与分发，后续再统一目录元数据。

### 全局任务反馈与 firewalld 安装验证修复（2026-07-27）

完成：

- 修复旧 HTTP 传输层只接受 `200`、把规范的 `202 Accepted` 任务创建响应错误显示为“红叉 + 操作成功”的问题；全部异步任务创建接口统一按成功的 2xx 处理。
- 顶栏新增全局运行任务数字框；点击后展示组件、操作、当前阶段、百分比和进度条，再点击任务可直接打开实时任务抽屉。
- 任务从运行态进入终态后显示右上角成功/失败通知；安全页自动重新读取防火墙状态和规则，不再要求手工刷新。
- 安装卡片在任务创建后立即切换为“查看安装进度”，任务数从 `0 → 1 → 0` 实时变化。
- 软件任务日志拉取增加单任务并发合并，避免 SSE 与定时轮询同时使用同一游标造成日志重复拼接。
- firewalld 验证改为读取 Python 模块版本，不再用会隐式连接 D-Bus 的 `firewall-cmd --version` 误判离线/容器环境。
- 最新内嵌前端 SHA-256：`054dfb8cb2cea4972332838d89c9e4a996fbe8fdced8e6eb574655e1d372c8e0`。

验证：

- 前端 `npm run typecheck` 和 `npm run buildNocnd` 通过；后端软件任务、脚本注册表与软件处理器重点测试通过。
- firewalld 全部 Shell 脚本通过 `bash -n`，组件 `files.sha256` 全量校验通过。
- Docker 测试容器重新构建后为 healthy；Debian bookworm arm64 中真实安装 firewalld `1.3.3` 成功并保持关闭。
- 浏览器实测确认 202 不再产生错误提示；成功消息类为 `el-message--success`，图标路径为圆形对勾。
- 浏览器实测确认顶栏任务入口、运行任务列表、实时抽屉、完成通知及安全页自动切换为 `firewalld / 规则持久化`。

尚未完成：

- 当前顶栏首先统一软件安装与维护任务；网站、数据库和计划任务仍各自使用独立任务模型，后续需要统一聚合接口。
- Docker 验证不等于真实 systemd 防火墙验收，仍需 Ubuntu、Debian、Rocky/AlmaLinux 实机测试启用、规则持久化和重启恢复。

### firewalld 默认安装与主框架修复（2026-07-27）

完成：

- 安全页检测不到受支持的防火墙时，提供默认 firewalld 安装入口和明确说明。
- 安装请求复用持久化软件任务、事件流和日志抽屉；HTTP 端不接受任意包名或 Shell。
- 新增 firewalld 独立脚本包，含预检、安装、验证、回滚和卸载，兼容常用 Debian/RPM/zypper 系统与 amd64/arm64。
- 安装后保持关闭，首次启用时先保护面板管理端口，避免防火墙安装导致面板失联。
- 运行日志已纳入统一顶部栏/侧边栏，侧栏底部品牌卡已移除。
- 最新内嵌前端 SHA-256 已由后续任务反馈修复批次更新为：`054dfb8cb2cea4972332838d89c9e4a996fbe8fdced8e6eb574655e1d372c8e0`。

验证：

- Panel `go test ./...` 全量通过；firewalld Shell 语法、包清单、动作权限和文件 SHA-256 校验通过。
- 前端 `npm run typecheck`、2 个 Vitest 用例和 `npm run buildNocnd` 通过。

尚未完成：

- 需要在 Ubuntu、Debian、Rocky/AlmaLinux 实机验证安装、默认关闭、首次启用端口保护和重启持久化。

### 四组件结构化配置与安全发布闭环（2026-07-27）

完成：

- Center/Panel 严格清单新增配对的 `configGet/configApply` 动作与独立超时，四组件继续保持一组件一脚本。
- 配置 API 只接收固定字段，不接收任意文本或 Shell；服务端校验类型、范围、枚举、字段完整性和 PHP-FPM 进程/上传大小关系。
- 当前值读取使用固定键值协议、64 KiB 上限和 SHA-256 指纹；未知、重复、缺失或越权字段会被拒绝，密码/路径/监听配置不回显。
- 发布使用预览指纹防止并发覆盖；每次先创建 `0700` 快照并保留最近 20 份，再经 Nginx/MySQL/PHP-FPM/Redis 原生语法检查后原子替换。
- 运行中的 Nginx/PHP-FPM 平滑重载，MySQL/Redis 重启；停止状态保持停止。发布或服务动作失败、取消与正常终止会恢复快照。
- 配置变更进入持久化任务、组件互斥、SSE、日志、取消、重启核验和操作审计，且不会改动安装标记、版本或原安装日志。
- 前端新增配置抽屉、动态表单、版本/来源、字段级差异预览、生效方式提示和配置任务阶段。
- Center 包已同步至离线包；内嵌前端 SHA-256 为 `ab6eb6c6324361fbc76b4271f4a78b0af3047184c38e1ed1602588019e47351f`。

验证：

- Panel 全量 Go 测试通过，当前共有 232 个 Go 测试函数；Center 全量 Go 测试和四组件/内置脚本 `bash -n` 通过。
- 前端 `npm run typecheck`、2 个 Vitest 用例和 `npm run build` 通过；内嵌 ZIP 完整性校验通过。

尚未完成：

- 本机无真实 systemd/四组件环境，仍需 Ubuntu 22.04/24.04 干净机验证语法检查、reload/restart、超时、取消和故障回滚。
- 自动快照可用于失败回滚，但历史快照列表、差异查看和管理员手动恢复页面尚未开发。

### 四组件服务控制与运行状态闭环（2026-07-27）

完成：

- Center/Panel 组件清单新增 `status`、`start`、`stop`、`restart` 和可选 `reload` 动作及独立超时，部分声明会被拒绝。
- Nginx、MySQL 8、PHP-FPM 和 Redis 新增固定 systemd 单元的状态、启动、停止和重启脚本；Nginx/PHP-FPM 在语法检查通过后支持平滑重载。
- MySQL 与 Redis 不宣传不可靠的 reload 能力，API 和前端明确引导使用重启。
- 状态探测输出采用固定字段、64 KiB 上限、身份/状态/版本格式校验；只执行已验证组件包中的 `status` 动作。
- 服务变更复用持久化软件任务、组件互斥、SSE、增量日志、取消、优雅停服中断和重启恢复，不从 HTTP 接口拼接任意 `systemctl` 命令。
- 服务动作不会写入或覆盖安装状态、已安装版本和原安装日志；只有安装生命周期动作可以修改软件安装记录。
- 新增管理员服务状态与控制 API，并进入现有 CSRF、限流和持久化审计链。
- 前端软件卡片展示运行/停止/失败状态和运行版本，提供启动、停止、重启、平滑重载、刷新及统一任务抽屉。
- Center 生产包已重新生成并同步到 Panel 离线包；前端生产 ZIP 已同步到 `webui/app.zip`，SHA-256 为 `547a5fc9d5eb7d478cdf186abeb659590df714b7e71be1545f2233fefea0e878`。

验证：

- Panel `go test ./...`、软件任务/脚本/处理器重点包 race、`go vet ./...`、生产二进制构建、内置包校验、Shell 语法检查和 `git diff --check` 通过。
- Center `go test ./...`、`go vet ./...`、CLI 构建、清单校验、动作执行权限和 Shell 语法检查通过。
- 当前共有 224 个 Go 测试函数；新增覆盖严格状态解析、恶意/重复字段拒绝、服务任务进度、未安装拒绝、服务动作不改变安装记录、输出上限和管理员权限。
- 前端 `npm run typecheck`、2 个 Vitest 用例和 `npm run build` 通过。

尚未完成：

- 本机没有真实 systemd 环境，本轮没有实际启动或停止宿主机服务；必须在 Ubuntu 22.04/24.04 amd64 干净机验证四组件运行状态、停止后恢复、取消、超时、Panel 重启和失败状态。
- MySQL 和 Redis 不具备通用安全的 systemd reload 语义，因此仅支持启动、停止和重启；配置模块已按组件完成校验和重启策略。
- 服务状态尚未进入监控告警规则；配置快照、差异预览和原子发布已在后续 `DEV-P1-17` 完成。

### 默认 IP/HTTP 与可选 HTTPS 访问闭环（2026-07-26）

完成：

- 明确 HTTP 是默认且长期保留的管理入口，默认监听 `0.0.0.0:8089`，用户无需域名即可使用 `http://服务器IP:8089` 访问。
- HTTPS 改为默认关闭的独立监听器，启用后与 HTTP 使用不同端口；不关闭 HTTP、不强制跳转，也不发送会让 HTTP 入口失效的 HSTS。
- 服务启动会在接受请求前同时预绑定 HTTP 和可选 HTTPS 套接字；HTTPS 强制 TLS 1.2 以上，并校验证书/私钥匹配、有效期和证书中的 DNS/IP SAN。
- 新增管理员面板访问配置 API，保存前检查监听 IP、端口范围/冲突、证书、私钥、可信代理 IP/CIDR 和候选端口占用。
- 新端口先写入受保护的防火墙放行规则；配置写入采用 `0600` 临时文件、`fsync` 和原子替换，失败会关闭候选端口并回滚本次新建的防火墙规则。
- 可信代理默认空；未明确信任的连接不能通过伪造 `X-Forwarded-Proto` 获得 Secure Cookie，也不能通过转发头伪造审计来源 IP。
- Gin 客户端地址解析、会话 Cookie 和审计来源统一使用可信代理边界；新增 CSP、禁止嵌入、MIME 嗅探、Referrer 和浏览器能力限制响应头。
- 前端新增“面板访问方式”设置，展示 HTTP/HTTPS 访问地址、重启待生效状态、证书状态、IP SAN 提示和可信代理风险说明。
- 生产前端 ZIP 已同步到后端 `webui/app.zip`，SHA-256 为 `4a071581b2d3cfd3b3bbd1372b421946a730a177f0f7aa6ac9323a1c0349b7e3`。

验证：

- 后端 `go test -count=1 ./...`、相关包 `go test -race`、`go vet ./...`、`go build ./...`、Shell 语法检查和 `git diff --check` 通过。
- 当前共有 216 个 Go 测试函数；新增覆盖默认 IP/HTTP、监听地址、HTTP/HTTPS 独立性、证书有效期与 IP SAN、可信/不可信代理、Cookie、安全响应头、原子配置和安全权限。
- 前端 `npm run typecheck`、2 个 Vitest 用例和 `npm run buildNocnd` 通过；后端嵌入包已包含新的访问设置页面。

尚未完成：

- 保存访问配置后需要管理员确认云安全组/外部防火墙并重启 Panel 服务；systemd 自动重启、启动失败自动恢复上一版配置和运行中无损换端口尚未实现。
- 默认监听 `0.0.0.0` 只代表所有 IPv4 网卡，是否能从公网访问仍由服务器路由、云安全组和主机防火墙决定。
- HTTPS 使用 IP 访问时证书必须含对应 IP SAN，否则浏览器会告警；公网 IP SAN、自签名证书、Nginx/Caddy 反向代理和 IPv6 仍需实机矩阵。

### 网站删除安全与整站备份闭环（2026-07-26）

完成：

- 网站删除改为持久化异步任务，必须精确输入网站名；完整快照创建并通过 SHA-256 校验后才允许移除网站。
- 默认只删除 Nginx 与证书状态并保留业务文件；选择删除文件时，服务端重新验证网站目录严格位于配置的 Web 根目录下，拒绝根目录、越界路径和任一路径组件中的符号链接。
- 网站与证书目录删除前先原子移动到私有暂存区；数据库或 Nginx 配置发布失败会回移目录并回滚网站记录，不再忽略目录删除错误。
- 新增持久化网站备份、恢复和安全删除任务，包含排队、进度、日志、主动取消、资源锁、Panel 重启中断恢复和前端轮询。
- 整站备份包包含经校验的网站元数据、Nginx 配置快照、网站文件逐项摘要及一个可选 MySQL 转储；证书私钥明确不进入备份。
- 归档完全使用 Go 标准库生成与解压，不调用 Shell；拒绝绝对路径、目录穿越、重复条目、未声明数据库、危险符号链接、特殊文件和路径父级符号链接。
- 备份设置 20 GiB/20 万条目默认上限、磁盘预留检查、整包 SHA-256、恢复前安全快照、恢复源归属校验和数据库操作互斥。
- 备份默认保留 30 天并每天 `04:15` 自动清理；活动恢复正在使用的备份不会被删除，下载和删除均要求管理员并进入敏感审计。
- 前端新增站点级和全局整站备份管理，支持选择一个 MySQL 数据库、任务进度/取消/日志、下载、精确确认恢复、删除前文件处理选择，以及已删除站点从全局快照恢复。
- 生产前端 ZIP 已同步到后端 `webui/app.zip`，两端 SHA-256 均为 `81f9731019c730a6e10c4c993c8f7fe9cb6f0757998772593ddd501e4c2b2e8e`。

验证：

- 后端 `go test -count=1 ./...`、网站/网站任务/处理器/中间件重点包 `go test -race`、`go vet ./...` 和生产二进制构建通过。
- 当前共有 206 个 Go 测试函数，新增覆盖备份—恢复—删除—再恢复、关联数据库恢复、主动取消、重启中断、归档穿越/危险链接拒绝、篡改拒绝、保留清理保护活动恢复、Nginx 失败目录回移和删除确认。
- 前端 `npm run typecheck`、2 个 Vitest 用例和 `npm run build` 通过；后端嵌入包包含整站备份管理页面。
- 仓库仍有 14 个 Bats 安装器契约；本轮未修改安装 Shell，当前开发机未安装 Bats，未重复执行。

尚未完成：

- 当前每个整站备份最多关联一个 MySQL 数据库，数据库恢复沿用现有“恢复前安全快照 + 手动回退”策略；多数据库依赖图和数据库失败自动回滚进入后续增强。
- 证书私钥因备份包未加密而不纳入整站备份；恢复后需要重新签发/部署证书。
- Panel 在文件原子切换的极短窗口被强制断电时，只能依靠已登记安全快照人工恢复；需要在真实 Linux 上补充断电故障注入与恢复演练。
- 仍需在 Ubuntu 22.04/24.04 + Nginx + MySQL 8 环境验证大文件、符号链接、磁盘写满、长任务取消和反向代理站点矩阵。

### 计划任务安全模板与主动取消（2026-07-26）

完成：

- 计划任务新增“安全模板”和“高级 Shell”两种类型；新增磁盘使用报告、服务状态检查和网站目录容量三个白名单模板。
- 模板使用固定可执行文件和参数白名单直接启动进程，不拼接 Shell；网站目录只能选择配置的网站根目录下的安全名称。
- 高级 Shell 保留向后兼容，但创建和修改时必须由管理员显式确认风险；执行环境继续使用最小变量和受控 `PATH`。
- 运行实例使用独立执行 ID 跟踪，前端可主动取消；取消会终止整个进程组，Panel 重启会把遗留运行记录恢复为 `PANEL_RESTARTED` 中断状态。
- 输出继续限制为 1 MiB，并新增密码、令牌、Cookie、Bearer 凭据和控制字符脱敏。
- 执行日志支持状态和时间范围筛选、CSV 导出、默认 30 天自动清理和管理员手动清理；运行中记录不会被清理。
- 失败和超时可按任务配置进入现有监控通知中心，复用加密 Webhook、事件记录和投递记录，不把任务输出发送到通知端点。
- 前端完成模板元数据驱动表单、结构化参数、高级 Shell 风险确认、失败通知开关、真实运行状态、主动取消、日志筛选/导出/清理。
- 生产前端 ZIP 已同步到后端 `webui/app.zip`，两端 SHA-256 均为 `93e395a19471dd3a18d4dc6fa180861d172b8efd43e496abd8bd33587007cad3`。

验证：

- 后端 `go test -count=1 ./...`、计划任务/监控/处理器/中间件重点包 `go test -race`、`go vet ./...` 和生产二进制构建通过。
- 当前共有 206 个 Go 测试函数；计划任务批次新增覆盖模板参数注入拒绝、无 Shell 执行、序列化参数更新、主动取消、重启恢复、保留清理、输出脱敏、通知投递、权限和 CSV 公式注入防护。
- 前端 `npm run typecheck`、2 个 Vitest 用例和 `npm run build` 通过；后端嵌入包校验通过。
- 仓库仍有 14 个 Bats 安装器契约；本轮未修改 Shell 安装器，当前开发机未安装 Bats，未重复执行。

尚未完成：

- 高级 Shell 仍由 Panel 特权进程执行，只通过管理员显式确认、最小环境、超时和进程组终止降低风险；长期需要拆分低权限 API 与白名单特权执行器。
- 安全模板仍是首批三个；数据库/网站备份、证书检查和受控 HTTP 探测等模板需要结合统一任务与凭据模型继续扩展。
- 仍需在 Ubuntu 22.04/24.04 实机验证 systemctl 服务名差异、进程树 TERM/KILL、Panel 重启、磁盘写满、通知端点故障和长任务取消矩阵。

### 实时日志中心可靠性闭环（2026-07-26）

完成：

- 移除未接入主流程的旧安装 WebSocket 原型和废弃增强安装器，消除读锁下删除客户端、共享状态竞态、固定日志目录和空进度解析逻辑。
- 新增独立 `RuntimeLogEntry` 模型和 SQLite 日志管理器，面板标准日志与 HTTP 访问日志进入 4096 条有界队列，按 100 条或 100ms 批量持久化。
- 停服时停止接收新日志、等待已接受队列并刷新数据库；队列过载、持久化失败和停服期间拒绝都会进入丢弃计数。
- 新增稳定单调游标、最新/向前/向后增量查询、级别/来源/关键词/时间筛选、来源统计和 1～3650 天可配置保留清理。
- 写入前移除控制字符、限制单条 4096 字节，并对 Authorization、Bearer、Cookie、密码、Token 和 Secret 做统一脱敏。
- 实时协议改为管理员认证的 SSE；服务端先订阅再回填数据库游标，慢消费者主动断开后可通过 `Last-Event-ID` 无损续传。
- HTTP 访问日志只记录方法、路径、状态、耗时和客户端地址，不采集查询字符串；健康探针和日志 API 自身不产生循环噪声。
- 前端新增管理员“运行日志”页面，支持统计、筛选、历史范围、加载更早、暂停/恢复、自动滚动、复制、导出当前视图和连接状态。
- 生产前端 ZIP 已同步到后端 `webui/app.zip`，两端 SHA-256 均为 `b5a6d8fd5eed96c02a52c9728101efee093be3f47a6563f53c0292e6fe96d0ec`。

验证：

- 后端 `go test -count=1 ./...`、日志/处理器/路由重点包 `go test -race`、`go vet ./...` 和生产二进制构建通过。
- 当前共有 206 个 Go 测试函数；实时日志批次新增覆盖脱敏、筛选、稳定游标、停服刷新、慢消费者恢复、统计清理、SSE 续传、管理员权限和 HTTP 查询参数不落日志。
- 前端 `npm run typecheck`、2 个 Vitest 用例和 `npm run build` 通过；后端嵌入包校验通过。
- 仓库仍有 14 个 Bats 安装器契约；本轮未修改 Shell，当前开发机未安装 Bats，未重复执行。

尚未完成：

- 当前首批生产日志源为面板标准库日志和 HTTP 访问摘要；journald、Nginx、PHP、MySQL 与组件脚本日志需通过受控适配器接入。
- 仍需在真实 Linux 主机进行长连接、突发流量、SQLite 增长、磁盘写满、重启续传和反向代理缓冲矩阵。
- 诊断包导出、错误追踪 ID、日志压缩归档和远端日志系统对接进入后续增强。

### 监控与通知告警闭环（2026-07-26）

完成：

- 新增 CPU、内存、根分区、系统负载、网络与磁盘 I/O 分钟级样本，默认保留 30 天并按计划自动清理。
- 新增阈值规则、连续采样、触发与恢复滞回、提醒冷却、`normal → pending → firing` 状态机及恢复事件。
- 静默期间仍保存事件和状态但不对外发送；解除静默后继续按状态和冷却周期评估。
- 新增告警事件、通知投递记录和独立 365 天保留策略，历史指标查询始终返回时间范围内最新一段并按时间正序展示。
- 新增公网 HTTPS Webhook 通知通道、HMAC-SHA256 签名和测试发送；完整 URL/查询令牌与签名密钥使用实例凭据密钥加密且 API 不回显。
- Webhook 禁止本机、私网、链路本地、保留地址、重定向和代理，并在实际连接前重新解析地址，降低 SSRF 与 DNS 重绑定风险。
- 监控管理路由仅管理员可用，所有规则、静默与通道变更进入持久化敏感操作审计。
- 前端新增管理员“监控告警”页面，包含 24 小时趋势、实时摘要、规则状态、事件筛选、静默、通知通道和投递记录。
- 生产前端 ZIP 已同步到后端 `webui/app.zip`，两端 SHA-256 均为 `d95da0d37a35e4aa42e5566586c8d4d8ba219993a5bfd5afcf6a39dcbae65614`。

验证：

- 后端 `go test -count=1 ./...`、监控/路由重点包 `go test -race`、`go vet ./...` 和 `go build ./...` 通过。
- 当前全仓共有 206 个 Go 测试函数；监控批次新增用例覆盖连续触发、滞回区提醒、恢复、静默抑制、规则状态、凭据加密、SSRF 拒绝、保留清理和历史窗口。
- 前端 `npm run typecheck`、2 个 Vitest 用例和 `npm run buildNocnd` 通过。
- 仓库仍有 14 个 Bats 安装器契约；本轮未改 Shell，当前开发机未安装 Bats，未重复执行。

尚未完成：

- 需要在真实 Linux 主机进行 7×24 小时采集、SQLite 增长、网络抖动和通知端点故障验收。
- 当前提供通用安全 Webhook；邮件、钉钉、飞书、企业微信的原生消息格式与重试队列进入后续增强。
- 服务存活、证书到期、网站可用性和安装任务失败尚未统一接入规则引擎。

### 会话安全与首次登录保护（2026-07-26）

完成：

- 新增服务端 `UserSession` 会话登记表；JWT 只保存随机会话 ID 和安全版本，令牌本体不落库。
- 每次认证同时校验 JWT、用户安全版本和服务端会话状态；旧无状态 JWT、过期、已吊销和版本失效会话立即返回 401。
- 登录记录来源 IP、User-Agent、创建/最近活动/到期时间；最近活动写入带 5 分钟节流，过期/吊销记录自动保留清理。
- 退出会吊销当前会话；安全设置支持查看有效设备、吊销指定设备和一键退出其他设备。
- 修改密码要求当前密码；成功后增加安全版本、清除首次登录标记、事务内吊销全部会话并要求重新登录。
- 初始管理员和新建账号默认标记为必须改密；中间件仅放行改密、退出和只读安全状态接口。
- 实现 RFC 6238 TOTP，支持二维码/手动密钥、启用确认、停用和恢复码重建。
- TOTP 密钥使用实例 AES-256-GCM 凭据密钥加密；恢复码仅保存派生 HMAC 摘要，每条只能消费一次。
- 登录页支持账号密码 → 动态口令/恢复码两步流程；同一 30 秒 TOTP 计数器不能重复创建会话。
- 前端新增强制改密页和账号安全页面；恢复码只显示一次，停用 TOTP 或修改密码会让全部会话立即失效。
- 会话、TOTP、恢复码、改密和吊销接口进入现有敏感操作审计链，不记录请求正文或凭据。

验证：

- 自动化覆盖服务端会话创建/校验/即时吊销、旧 JWT 拒绝、退出后令牌重用拒绝、改密后全会话失效和首次登录路由限制。
- 自动化覆盖 TOTP 加密落库、启用确认、时间窗口、计数器重放拒绝、恢复码单次消费和明文不落库。
- 后端 `go test ./...` 全量通过；当前全仓共 206 个 Go 测试函数。
- 会话服务、路由和中间件重点包 `go test -race` 以及全量 `go vet ./...` 通过。
- 前端 `npm run typecheck`、`npm test` 和 `npm run build` 通过，生产 ZIP 已同步至后端嵌入包。
- 仓库保留 14 个 Bats 用例和 2 个前端 Vitest 用例；当前开发机未安装 Bats，本轮未重复执行。

尚未完成：

- 当前为 v1.0 单管理员模型；多管理员的独立角色、设备策略和强制 MFA 策略进入 v1.1。
- 尚未实现 Passkey/WebAuthn、受信任设备和管理员远程强制重置 MFA。
- 生产环境仍需完成实例凭据密钥备份/轮换演练；丢失密钥会导致已配置 TOTP 无法解密。

## 下一个开发项目

`配置历史恢复与组件健康告警闭环`：列出配置快照、查看字段级差异、提交受审计的手动恢复任务，并把 Nginx、MySQL、PHP-FPM、Redis 的停止/失败/恢复状态接入现有监控规则与通知中心。

## Ubuntu 24.04 原生测试进度（2026-07-27）

| 项目 | 状态 | 实机结果 |
| --- | --- | --- |
| Panel 原生部署 | 已完成 | systemd active，`0.0.0.0:8089`，局域网 HTTP 与健康检查通过 |
| Center 原生部署 | 已完成 | systemd active，`127.0.0.1:8189`，四类稳定包已签名发布 |
| Center 信任链 | 已完成 | Panel 完成公钥、签名、SHA-256、逐文件校验和缓存 |
| firewalld 安装 | 已完成 | 任务成功，离线配置校验通过，无 `INVALID_PROTOCOL: esp` |
| firewalld 启用 | 已完成 | 8089 先保护后启动，规则持久化；22/80/5244/8089 外部可达 |
| Nginx 1.28.2 | 已完成 | 首次故障自动回滚；Center `1.0.2` 修复后安装、systemd、HTTP 均通过 |
| MySQL 8.0 | 核心闭环已完成 | Center `1.0.6` 安装 MySQL 8.0.45 成功；systemd、3306 回环监听、自动连接、建库/同名账户、查看/轮换密码、实连和删除通过；重复安装/备份恢复/故障矩阵待补 |
| PHP | 待实机验证 | Center 包已发布，尚未执行 8.1/8.2/8.3 矩阵 |
| Redis 7.4.8 | 待实机验证 | Center 包已发布，尚未执行安装/配置/重启矩阵 |
| 重启恢复 | 待验证 | 需验证 Panel、Center、Nginx、firewalld 和规则开机恢复 |

## Center 控制软件商城（2026-07-27）

| 项目 | 状态 | 当前结果 |
| --- | --- | --- |
| Center 商城目录 | 已完成 | 独立持久化，Ed25519 签名，默认管理 5 个应用 |
| Center 管理界面 | 已完成 | 可控制显示、安装开关、版本、推荐版本、通道和排序 |
| Panel 自动同步 | 已完成 | 启动立即同步，默认每 15 分钟同步，支持 ETag |
| 安全校验 | 已完成 | 固定公钥、签名、摘要和目录字段全部校验后才事务入库 |
| Center 离线 | 已完成 | 保留最后可信快照并显示来源、时间、过期和错误状态 |
| 安装权限 | 已完成 | 只允许安装当前 Center 目录中已启用的应用和版本 |
| 已安装软件保护 | 已完成 | Center 移除后停止新装，但本机已安装实例仍可管理和卸载 |
| 前端商城 | 已完成 | 真实的全部/已安装/可升级列表、手动同步和推荐升级 |
| 自动化验证 | 已完成 | Center、Panel 全量 Go 测试及前端类型、Vitest、生产构建通过 |
| 原生端到端验收 | 待执行 | 需部署至 Ubuntu 测试机验证断网、隐藏、停装和恢复同步 |
